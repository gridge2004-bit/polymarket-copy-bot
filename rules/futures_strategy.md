# Futures Trading Rules — Source of Truth (NQ/MNQ + options)

> This is YOUR trading plan. The trading skills (premarket-rundown,
> post-trade-review, weekly-autopsy, trade-journal) grade against this file. If
> a rule isn't written here, it can't be enforced. Fill in every `_____`.
>
> Be specific. "Trade good setups" is ungradeable. "Long NQ on a VWAP reclaim in
> the first 90 min, 2-pt stop, 4-pt target" is gradeable.

## Instruments & sizing
- **Primary:** NQ / MNQ. **Secondary:** options on `_____`.
- **Contract sizing:** start `_____` MNQ; scale to NQ when `_____`.
- **Per-trade risk:** max `_____` points / `$_____` per trade.
- **Daily loss limit (hard stop, walk away):** `$_____` or `_____` points.
- **Daily profit target / "green-to-flat" rule:** `_____`.
- **Max trades/day:** `_____` (overtrading is the #1 futures killer).

## Session windows (ET)
- **A+ window:** `_____` (e.g. 9:30–11:00 cash open).
- **Avoid / chop window:** `_____` (e.g. 12:00–14:00 lunch).
- **No new trades:** `_____` minutes before red-folder news; `_____` before close.

## Setups I'm allowed to trade (define each)
1. **Opening drive / ORB:** trigger `_____`, stop `_____`, target `_____`.
2. **VWAP reclaim / rejection:** trigger `_____`, stop `_____`, target `_____`.
3. **Prior-day H/L or value-area edge:** `_____`.
4. **Trend pullback (e.g. to a moving avg):** `_____`.
> If a trade isn't one of these, it's a RED trade by definition.

## Macro / news rules (futures-specific)
- **Red-folder events** (CPI, FOMC, NFP, PCE, PMIs): `_____` (flatten before? trade the reaction only? size down?).
- **Fed speakers / auctions:** `_____`.
- **Bias inputs each morning:** overnight ES/NQ trend, US10Y yields, DXY, BTC/ETH
  risk tone, sector leadership. Bias is `_____` until `_____` invalidates it.

## Options rules (when used)
- **Only trade options when:** `_____` (e.g. defined-risk directional, event vol).
- **Max premium at risk/day:** `$_____`. **No** holding `0DTE` past `_____`.

## Risk & psychology hard limits
- After `_____` consecutive losses → stop for the day.
- No revenge trades, no moving stops, no adding to losers. (Each = automatic RED.)
- Sizing up is only allowed after `_____`, never to "make it back."

## What "following the plan" means (for grading)
- **GREEN:** one of the defined setups, in an allowed window, sized to the risk
  rule, stop honored, not into blocked news.
- **RED:** no defined setup, wrong window, oversized, stop moved/widened, revenge
  entry, or traded through a blocked news window — **regardless of P&L.**

## Change log
- _YYYY-MM-DD_: initial NQ/MNQ rules drafted.
